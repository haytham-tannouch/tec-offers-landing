<section class="relative min-h-screen flex items-center justify-center bg-gradient-to-br from-blue-900 to-purple-900 text-white overflow-hidden">
  <div class="container mx-auto px-4 py-16 relative z-10">
    <div class="max-w-4xl mx-auto text-center">
      <h1 id="hero-title" class="text-5xl md:text-6xl font-bold mb-6">
        Votre Site Web Professionnel <span class="text-yellow-400">Gratuit</span>
      </h1>
      <p class="text-xl md:text-2xl mb-8 text-gray-200">
        Transformez votre présence en ligne avec un site web moderne, rapide et optimisé pour le référencement.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="#checkout" class="bg-yellow-400 text-blue-900 px-8 py-4 rounded-lg font-bold text-lg hover:bg-yellow-300 transition-colors">
          Obtenir Mon Site Gratuit
        </a>
        <a href="#realisations" class="border-2 border-white px-8 py-4 rounded-lg font-bold text-lg hover:bg-white hover:text-blue-900 transition-colors">
          Voir Nos Réalisations
        </a>
      </div>
    </div>
  </div>
  <div class="absolute inset-0 bg-grid-pattern opacity-10"></div>
</section>

<script>
  window.addEventListener('DOMContentLoaded', () => {
    document.getElementById('hero-title').classList.add('animate-fade-in');
  });
</script>

<style>
  .bg-grid-pattern {
    background-image: linear-gradient(to right, rgba(255,255,255,0.1) 1px, transparent 1px),
                      linear-gradient(to bottom, rgba(255,255,255,0.1) 1px, transparent 1px);
    background-size: 40px 40px;
  }

  @keyframes fade-in {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .animate-fade-in {
    animation: fade-in 1s ease-out forwards;
  }
</style> 