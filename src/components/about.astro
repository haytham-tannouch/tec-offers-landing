---
import aboutImg from "../assets/images/about.jpg";
---

<section class="relative md:py-24 py-16">
  <div class="container relative">
    <div class="grid md:grid-cols-12 grid-cols-1 items-center gap-6">
      <div class="md:col-span-6">
        <div class="lg:me-8">
          <div class="relative">
            <img
              src={aboutImg.src}
              class="rounded-full shadow-sm dark:shadow-gray-700"
              alt=""
            />

            <div
              class="absolute top-1/2 -translate-y-1/2 start-0 end-0 mx-auto size-56 flex justify-center items-center bg-white dark:bg-slate-900 rounded-full shadow-sm dark:shadow-gray-700"
            >
              <div class="text-center">
                <span class="text-teal-500 text-2xl font-semibold mb-0 block"
                  ><span
                    class="counter-value text-6xl font-semibold"
                    data-target="100">1</span
                  >+</span
                >
                <span class="font-semibold block mt-2"
                  >Sites Web <br /> Créés</span
                >
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="md:col-span-6">
        <div class="lg:ms-8">
          <h6 class="text-teal-500 text-sm font-semibold uppercase mb-2">
            Notre Expertise
          </h6>
          <h3 class="font-semibold text-2xl leading-normal mb-4">
            Nous créons des sites web <br /> professionnels pour <br /> entrepreneurs
            ambitieux.
          </h3>

          <p class="text-slate-400 max-w-xl mb-6">
            Notre mission est de rendre la création de site web accessible à
            tous les entrepreneurs. Nous croyons que chaque entreprise mérite
            une présence en ligne professionnelle, c'est pourquoi nous offrons
            gratuitement nos services de création de site web.
          </p>

          <a
            href="#contact"
            class="h-10 px-6 tracking-wide inline-flex items-center justify-center font-medium rounded-md bg-teal-500 text-white"
            >Réserver mon appel découverte <i
              class="mdi mdi-chevron-right align-middle ms-0.5"></i></a
          >
        </div>
      </div>
    </div>
  </div>
</section>

<script type="module">
  document.addEventListener("DOMContentLoaded", function () {
    try {
      const counters = document.querySelectorAll(".counter-value");
      const duration = 2000;

      counters.forEach((counter) => {
        const target = +counter.getAttribute("data-target");
        let count = 0;
        const step = target / (duration / 20);

        const updateCount = () => {
          if (count < target) {
            count += step;
            counter.innerText = Math.floor(count);
            setTimeout(updateCount, 20);
          } else {
            counter.innerText = target;
          }
        };

        updateCount();
      });
    } catch (err) {
      console.error("Error in counter script:", err);
    }
  });
</script>
